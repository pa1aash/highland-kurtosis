# Repository Audit: g4-lattice-mcs

**Audited:** 2026-03-22
**Scope:** Full codebase scan of Geant4 MCS simulation for 3D-printed lattice materials

---

## 1. Project Structure

```
g4-lattice-mcs-main/
├── CMakeLists.txt              # Build system (Geant4 + optional CADMesh)
├── MCSHighland.cc              # Main executable entry point
├── README.md                   # Project documentation
├── .gitignore                  # Excludes build/, data/, results/, *.root, *.stl
│
├── include/                    # Geant4 header files (8 files)
│   ├── ActionInitialization.hh
│   ├── DetectorConstruction.hh
│   ├── DetectorMessenger.hh
│   ├── EventAction.hh
│   ├── PhysicsList.hh
│   ├── PrimaryGeneratorAction.hh
│   ├── RunAction.hh
│   └── SteppingAction.hh
│
├── src/                        # Geant4 source files (8 files)
│   ├── ActionInitialization.cc
│   ├── DetectorConstruction.cc   # ~844 lines, 7 geometry constructors
│   ├── DetectorMessenger.cc
│   ├── EventAction.cc
│   ├── PhysicsList.cc
│   ├── PrimaryGeneratorAction.cc
│   ├── RunAction.cc
│   └── SteppingAction.cc
│
├── macros/                     # Geant4 macro files (6 files)
│   ├── init.mac                # Verbosity + rangeFactor=0.04
│   ├── vis.mac                 # OpenGL visualization
│   ├── run_solid_control.mac   # Solid PLA at 2/4/6 GeV, 1M events each
│   ├── run_air_control.mac     # Air-only at 2/4/6 GeV, 1M events each
│   ├── sweep1_physics_validation.mac   # Gyroid 40%, 4 GeV, rf=0.04
│   └── sweep1a_finest.mac              # Gyroid 40%, 4 GeV, rf=0.02
│   └── generated/              # Auto-generated by generate_macros.py (gitignored)
│
├── scripts/                    # Build/run infrastructure (6 files)
│   ├── install_geant4.sh       # Conda or source install helper
│   ├── generate_macros.py      # Generates 148 macro files across 6 sweeps
│   ├── generate_geometry.py    # STL generator for gyroid/voronoi/rectilinear
│   ├── ray_trace_sweep0.py     # Ray-trace path-length distributions
│   ├── run_all_sweeps.sh       # Master pipeline: build, generate, run, analyze
│   └── run_proposal_campaign.sh  # 27-config proposal campaign (50k events)
│
├── analysis/                   # Python analysis scripts (11 files)
│   ├── analyze_mcs.py          # Main analysis pipeline (ROOT → kurtosis + plots)
│   ├── generate_stls.py        # STL files for 3D printing
│   ├── visualize_stls.py       # Cross-section verification plots
│   ├── ray_trace_sweep0.py     # Ray-trace predictions (duplicate of scripts/)
│   ├── proposal_analysis.py    # Proposal-specific ROOT analysis
│   ├── proposal_figures.py     # Proposal publication figures
│   ├── generate_proposal_figures.py  # Additional proposal figures (fig 4-6)
│   ├── generate_theory_comparison.py # Highland vs mixture vs Geant4
│   ├── n_scaling_analysis.py   # 1/N scaling law analysis
│   ├── bl4s_predictions.py     # BL4S measurement predictions
│   └── universal_equation_final.py   # Universal equation validation
│
├── data/                       # Simulation data (partially populated)
│   └── sweep0/                 # Ray-trace results only
│       ├── sweep0_summary.json
│       ├── n_scaling_summary.json
│       ├── gyroid_period_summary.json
│       ├── gyroid_independent_summary.json
│       └── paths_*.npz (20 files, 5 geom x 4 infills)
│
├── proposal_targets/           # STL files + visualization for proposal
│   ├── solid_100pct.stl
│   ├── rectilinear_40pct.stl
│   ├── gyroid_40pct.stl
│   └── target_*.png (3 visualization PNGs)
│
├── proposal_figures/           # Proposal publication figures
│   ├── figure_4.png/.pdf       # N-scaling law
│   ├── figure_5.png/.pdf       # Angular distributions
│   ├── figure_6.png/.pdf       # Kurtosis vs infill
│   └── theory_same_mass_comparison.png/.pdf
│
├── make_all_figures.py         # NIM-A publication figures 2-7 (synthetic data)
├── make_figure1.py             # NIM-A Figure 1: geometry cross-sections
└── figure_*.png (7 PNG files)  # Generated NIM-A figures
```

### Files in wrong place
- `ray_trace_sweep0.py` exists in both `scripts/` and `analysis/`. The `scripts/` version is the authoritative one used by `run_all_sweeps.sh`; the `analysis/` version appears to be an older duplicate.

### Directories referenced in README that don't exist
- `stl_outputs/` — referenced in README but does not exist (gitignored, created by `analysis/generate_stls.py`)
- `results/` — referenced in README but does not exist (gitignored, created by analysis scripts)
- `geometry/` — referenced in CMakeLists.txt (line 42-46, copies `geometry/*.stl`) but does not exist (gitignored, created by `scripts/generate_geometry.py`)
- `macros/generated/` — created by `generate_macros.py` but gitignored

### Generated output files currently in repo
| File | Location | Type |
|------|----------|------|
| `sweep0_summary.json` | `data/sweep0/` | Ray-trace predictions for 21 configs |
| `n_scaling_summary.json` | `data/sweep0/` | Stacked-layer kurtosis scaling |
| `gyroid_period_summary.json` | `data/sweep0/` | Deterministic gyroid period sweep |
| `gyroid_independent_summary.json` | `data/sweep0/` | Independent gyroid period scaling |
| `paths_*.npz` (20 files) | `data/sweep0/` | Path-length distributions per config |
| `solid_100pct.stl` | `proposal_targets/` | Solid PLA target for 3D printing |
| `rectilinear_40pct.stl` | `proposal_targets/` | Rectilinear 40% target |
| `gyroid_40pct.stl` | `proposal_targets/` | Gyroid 40% target |
| `target_*.png` (3 files) | `proposal_targets/` | Target visualizations |
| `figure_{4,5,6}.{png,pdf}` | `proposal_figures/` | Proposal figures |
| `theory_same_mass_comparison.{png,pdf}` | `proposal_figures/` | Theory model comparison |
| `figure_{1-7}_*.png` (7 files) | root | NIM-A publication figures |

**No `.root` files exist in the repo** (all gitignored; Geant4 simulations have not been run or outputs were cleaned).

---

## 2. Geant4 Source Code Inventory

### MCSHighland.cc (main)
- **Lines:** 53
- **Purpose:** Entry point. Creates RunManager, initializes DetectorConstruction, PhysicsList, ActionInitialization. Supports interactive mode (no args) or batch mode (macro file as arg1, optional seed as arg2).
- **Random engine:** CLHEP::RanecuEngine, seeded from `time(nullptr)` or argv[2]

### DetectorConstruction.cc / .hh
- **Lines:** ~844 (largest file)
- **Class:** `DetectorConstruction` (note: typo in name, missing 'o' — "Construction")
- **Geometries implemented (7):**
  1. `kSolidPLA` — solid PLA block, volume `SolidPLA`
  2. `kAirOnly` — no target placed (null test)
  3. `kRectilinear` — CSG walls: `XWall` + `YWall` slabs in air `Target` volume
  4. `kHoneycomb` — voxelised 2D hex pattern, volume `HCWall`, pixel=0.1mm, extruded along z
  5. `kGyroid` — voxelised 3D TPMS via `|sin(kx)cos(ky)+sin(ky)cos(kz)+sin(kz)cos(kx)| < threshold`, volume `GVoxPLA`. Also supports STL import via CADMesh (`Gyroid` volume)
  6. `kCubic` — CSG walls in x/y/z: `CubXWall`, `CubYWall`, `CubZWall`
  7. `kVoronoi` — voxelised 3D CVT (centroidal Voronoi tessellation), volume `VVoxPLA`. Also supports STL import via CADMesh (`Voronoi` volume)
- **Materials defined:**
  - `PLA` — C3H4O2, density 1.24 g/cm3 (X0 ≈ 315 mm)
  - `G4_AIR` — from NIST database
  - **No silicon or tungsten defined**
- **Key configurable parameters:**

| Parameter | Member | Default | Messenger command |
|-----------|--------|---------|-------------------|
| Geometry type | `fGeometryType` | `kSolidPLA` | `/MCS/det/geometry` |
| Infill percent | `fInfillPercent` | 100.0 | `/MCS/det/infill` |
| Cell size | `fCellSize` | 4.0 mm | `/MCS/det/cellSize` |
| Wall thickness | `fWallThickness` | 0.4 mm | `/MCS/det/wallThickness` |
| Sample thickness (z) | `fSampleThickness` | 10.0 mm | `/MCS/det/sampleThickness` |
| Sample width (xy) | `fSampleWidth` | 20.0 mm | `/MCS/det/sampleWidth` |
| STL file path | `fSTLFile` | "" | `/MCS/det/stlFile` |

- **Target region:** 0.1 mm production cut (vs 1.0 mm global)
- **Step limits:** 0.1 mm `G4UserLimits` on all PLA volumes (0.5 mm on honeycomb voxels)
- **Infill tuning:** Honeycomb, gyroid, cubic, and voronoi all use bisection search to tune geometry parameters (cell spacing or threshold) to match requested infill percentage. Honeycomb and cubic use Monte Carlo sampling with beamSigma=5mm; gyroid uses uniform sampling.
- **Slab thickness:** `fSampleThickness` controls overall target thickness and is independent of lattice geometry — **solid PLA slab thickness CAN be changed via `/MCS/det/sampleThickness`**.
- **Note:** Cubic lattice constructor (lines 533-681) is correct: `yBounds` is initialized with `{-halfW}` at line 645, and `ix` is a proper loop variable in the `for` loop at line 657. Walls are placed in all three orientations (X, Y, Z) with proper bounds-based clipping to avoid overlaps.

### DetectorMessenger.cc / .hh
- **Class:** `DetectorMessenger`
- **7 commands exposed:** `/MCS/det/geometry`, `/MCS/det/infill`, `/MCS/det/cellSize`, `/MCS/det/wallThickness`, `/MCS/det/sampleThickness`, `/MCS/det/sampleWidth`, `/MCS/det/stlFile`
- All available in `G4State_PreInit` and `G4State_Idle`

### PhysicsList.cc / .hh
- **Class:** `PhysicsList`
- **EM physics:** `G4EmStandardPhysics_option4` (EMZ) — **hardcoded**, no command to switch
- **Other physics:** G4EmExtraPhysics, G4DecayPhysics, G4HadronElasticPhysics, G4HadronPhysicsFTFP_BERT, G4StoppingPhysics, G4IonPhysics, G4StepLimiterPhysics
- **MSC parameters (ConstructProcess):**
  - `MscRangeFactor` = `fRangeFactor` (default 0.04)
  - `MscSkin` = 3
  - `MscStepLimitType` = `fUseSafetyPlus`
  - `MscThetaLimit` = 0.2
- **Production cuts:** 1.0 mm global (0.1 mm in Target region set by DetectorConstruction)
- **Messenger commands:**

| Command | Type | Default |
|---------|------|---------|
| `/MCS/phys/rangeFactor` | double [0,1) | 0.04 |
| `/MCS/phys/mscStepMax` | double+unit | 0.1 mm |

### PrimaryGeneratorAction.cc / .hh
- **Class:** `PrimaryGeneratorAction`
- **Particle:** `e-` (electron) — **hardcoded via `FindParticle("e-")`**, no command to switch
- **Default energy:** 4.0 GeV (changeable via standard `/gun/energy`)
- **Beam direction:** (0, 0, 1) — along +z
- **Starting position:** (x, y, -50 mm) with Gaussian or pencil beam
- **Beam spot:** Gaussian sigma = 5.0 mm (default), or pencil (x=y=0)
- **Messenger commands:**

| Command | Type | Default |
|---------|------|---------|
| `/MCS/gun/pencilBeam` | bool | false |
| `/MCS/gun/beamSigma` | double+unit | 5 mm |

- **Note:** Energy is set via Geant4's built-in `/gun/energy`, NOT `/MCS/gun/energy`

### SteppingAction.cc / .hh
- **Class:** `SteppingAction`
- **Tracks only primary particle** (TrackID == 1)
- **Volume detection:** Uses lambda `isTargetVolume()` to check if volume name is one of: `Target`, `SolidPLA`, `PLAFill`, `XWall`, `YWall`, `HCWall`, `GVoxPLA`, `VVoxPLA`, `Gyroid`, `Voronoi`, `CubZWall`, `CubXWall`, `CubYWall`
- **PLA path accumulation:** `isPLAVolume()` — same list minus `Target` (which is air)
- **Entry detection:** first transition from non-target to target volume
- **Exit detection:** transition from target to `World` volume
- **Records:** entry position, entry momentum direction, exit momentum direction, exit energy, cumulative PLA path length

### EventAction.cc / .hh
- **Class:** `EventAction`
- **Ntuple columns filled (7):**

| Column index | Name | Unit | Description |
|-------------|------|------|-------------|
| 0 | `theta_x` | rad | Projected scattering angle x: `exit_tx - entry_tx` where `tx = px/pz` |
| 1 | `theta_y` | rad | Projected scattering angle y: `exit_ty - entry_ty` |
| 2 | `theta_space` | rad | Space angle: `sqrt(theta_x² + theta_y²)` |
| 3 | `energy_out` | GeV | Exit kinetic energy |
| 4 | `entry_x` | mm | Entry x position |
| 5 | `entry_y` | mm | Entry y position |
| 6 | `pla_path` | mm | Total PLA path length traversed |

- Only fills ntuple row if `fHasExit == true` (particle exited target)

### RunAction.cc / .hh
- **Class:** `RunAction`
- **Ntuple name:** `scattering`
- **Output format:** ROOT (`.root`) via G4AnalysisManager with ntuple merging enabled
- **Default filename:** `MCSOutput` (produces `MCSOutput.root`)
- **Messenger command:** `/MCS/output/fileName` — sets output filename (without extension)

### ActionInitialization.cc / .hh
- **Class:** `ActionInitialization`
- Wires together PrimaryGeneratorAction, RunAction, EventAction, SteppingAction

---

## 3. Macro System

### Hand-written macros

| Macro | Purpose | Key settings |
|-------|---------|-------------|
| `init.mac` | Shared initialization | Verbosity off, rangeFactor=0.04 |
| `vis.mac` | Interactive visualization | Rectilinear 40%, cellSize=2mm, OpenGL, 10 events |
| `run_solid_control.mac` | Solid PLA control runs | 3 energies (2/4/6 GeV), 1M events each |
| `run_air_control.mac` | Air-only null test | 3 energies, 1M events each |
| `sweep1_physics_validation.mac` | Physics validation | Gyroid 40%, 4 GeV, rf=0.04, 1M events |
| `sweep1a_finest.mac` | Finest step reference | Gyroid 40%, 4 GeV, rf=0.02, 1M events |
| `sweep2_infill_scan.mac` | Infill scan header | Gyroid, 4 GeV (file appears incomplete — only 11 lines, no `/run/beamOn`) |

### generate_macros.py (scripts/)

Generates **148 macro files** to `macros/generated/`:

| Sweep | Description | Count | Parameters |
|-------|-------------|-------|-----------|
| Primary matrix | 5 geom × 5 infills × 3 energies | 75 | All combinations |
| Controls | solid + air at 3 energies | 6 | 2/4/6 GeV |
| Sweep 1 | Physics validation (rangeFactor) | 4 | rf = 0.02, 0.04, 0.08, 0.20 |
| Sweep 2 | Infill scan | 20 | Gyroid 5-100% in steps of 5 |
| Sweep 3 | Geometry comparison | 15 | 5 geom × {20, 40, 60}% at 4 GeV |
| Sweep 4 | Cell size scaling | 5 | Gyroid 40%, cells 0.5-8.0 mm |
| Sweep 5 | Energy dependence | 18 | 3 infills × 6 energies (1-6 GeV) |
| Sweep 6 | Thickness optimization | 5 | Gyroid 20%, 2-40 mm thickness |

All generated macros use 1M events, wallThickness=0.4mm, sampleWidth=20mm, sampleThickness=10mm (except sweep 6).

### Complete macro command interface

| Command | Type | Default | Range/Candidates |
|---------|------|---------|-----------------|
| `/MCS/det/geometry` | string | `solid` | solid, air, rectilinear, honeycomb, gyroid, cubic, voronoi |
| `/MCS/det/infill` | double | 40 | 0-100 |
| `/MCS/det/cellSize` | double+unit | 4.0 mm | >0 |
| `/MCS/det/wallThickness` | double+unit | 0.4 mm | >0 |
| `/MCS/det/sampleThickness` | double+unit | 10 mm | >0 |
| `/MCS/det/sampleWidth` | double+unit | 20 mm | >0 |
| `/MCS/det/stlFile` | string | "" | path to .stl |
| `/MCS/phys/rangeFactor` | double | 0.04 | (0, 1) |
| `/MCS/phys/mscStepMax` | double+unit | 0.1 mm | >0 |
| `/MCS/output/fileName` | string | MCSOutput | no extension |
| `/MCS/gun/pencilBeam` | bool | false | true/false |
| `/MCS/gun/beamSigma` | double+unit | 5 mm | >0 |
| `/gun/energy` | double+unit | 4 GeV | Geant4 built-in |

---

## 4. Analysis Pipeline

### analyze_mcs.py (analysis/)
- **Purpose:** Main analysis pipeline — loads ROOT simulation output, computes scattering statistics, fits distributions, produces publication figures.
- **Input:** `--input-dir` (directory of .root files) or `--single-file`; optional `--sweep0` (sweep0_summary.json for universality plot)
- **Output:** `results/analysis_results.json`, `results/fig1_kurtosis_vs_infill.png`, `results/fig2_universality.png`, `results/map_*.png` (position-resolved maps)
- **Key functions:**
  - `highland_sigma_rad(x_over_X0, p_gev)` — Highland formula: `(13.6e-3/p) * sqrt(x/X0) * (1 + 0.038*ln(x/X0))`
  - `excess_kurtosis(data)` — **`scipy.stats.kurtosis(fisher=True, bias=False)`** — unbiased Fisher excess kurtosis estimator
  - `rms_width(data)` — `np.std(data, ddof=1)`
  - `core_gaussian_width(data, central_fraction=0.98)` — std of central 98% of data
  - `tail_fraction(data, n_sigma=3)` — fraction of data beyond 3σ
  - `fit_two_gaussian(data, n_bins=200)` — two-Gaussian mixture fit via `scipy.optimize.curve_fit`
  - `apply_cuts(data, beam_energy_gev, ...)` — applies energy, fiducial, and angle cuts; prints per-cut removal counts
  - `analyze_config(data, ...)` — main per-file analysis
  - `position_resolved_analysis(data, bin_size_mm=1.0)` — 2D map of σ and κ vs entry position
- **Quality cuts:** Configurable via command-line arguments (applied before `analyze_config`):
  - `--energy-cut-fraction` (default 0.9): keep events with `energy_out > fraction * beam_energy`
  - `--angle-cut-sigma` (default 10): keep events with `|theta_x| < N * sigma_Highland_solid`
  - `--fiducial-x` (default 5.0 mm): keep events with `|entry_x| < value`
  - `--fiducial-y` (default 10.0 mm): keep events with `|entry_y| < value`
  - `--beam-energy` (required when cuts applied): beam energy in GeV for Highland sigma calculation
  - `--no-cuts`: skip all cuts (backward compatible with previous behavior)
  - Highland sigma for angle cut uses solid PLA: `highland_sigma_rad(10.0/315.0, beam_energy)`
- **Constants:** `PLA_X0_MM = 315.0`, `PLA_X0_CM = 31.5`
- **Kurtosis error:** `sqrt(24/N)` (Gaussian approximation)

### proposal_analysis.py (analysis/)
- **Purpose:** Stricter analysis for proposal — applies energy, angle, and fiducial cuts.
- **Input:** ROOT files from `--data-dir` (default `data/proposal/`)
- **Output:** `--output-dir` (default `results/proposal/`) containing `proposal_summary.json`
- **Quality cuts (configurable via CLI):**
  - `--energy-cut-fraction` (default 0.9): `energy_out > fraction * p_GeV`
  - `--angle-cut-sigma` (default 10): `|theta_x| < N * sigma_Highland` (Nσ clip)
  - `--fiducial-x` (default 5.0 mm): `|entry_x| < value`
  - `--fiducial-y` (default 10.0 mm): `|entry_y| < value`
- **Kurtosis:** `scipy.stats.kurtosis(fisher=True, bias=False)` with 1000-resample bootstrap (seed=42)
- **Constants (now defaults):** `PLA_X0_MM = 315.0`, `SAMPLE_T_MM = 10.0`, `n_cut_sigma_DEFAULT = 10`, `FIDUCIAL_X_MM_DEFAULT = 5.0`, `FIDUCIAL_Y_MM_DEFAULT = 10.0`, `ENERGY_CUT_FRACTION_DEFAULT = 0.9`

### universal_equation_final.py (analysis/)
- **Purpose:** Validates universal kurtosis equation `κ = (3+κ_M)/f - 3` against Geant4 data.
- **Input:** ROOT files from `data/proposal/`, `data/sweep0/sweep0_summary.json`
- **Output:** Multi-table analysis (console)
- **Quality cuts:** Energy cut `>0.9*p`, angle cut `10*σ_Highland`, fiducial `|x|<5, |y|<10`
- **Bootstrap:** 2000 resamples, seed=42
- **PLA hit threshold:** `pla_path > 0.1 mm`

### ray_trace_sweep0.py (scripts/ only — duplicate in analysis/ deleted)
- **Purpose:** Pure-geometry ray-tracing to predict path-length distributions and kurtosis without Geant4.
- **Input:** None (generates geometry procedurally)
- **Output:** `data/sweep0/sweep0_summary.json`, `data/sweep0/paths_*.npz`, `data/sweep0/n_scaling_summary.json`, `data/sweep0/gyroid_{period,independent}_summary.json`
- **Key functions:** `raytrace_rectilinear()`, `raytrace_honeycomb()`, `raytrace_gyroid()`, `raytrace_cubic()`, `raytrace_voronoi()`, `raytrace_stacked_rectilinear()`, `raytrace_gyroid_stacked()`
- **Constants:** `BEAM_SIGMA = 5.0 mm`, `X0_PLA = 315.0 mm`, `NZ_BINS = 200`, `N_RAYS = 1,000,000`
- **Kurtosis formula:** `κ = 3 * Var(x/X0) / E[x/X0]²`

### generate_stls.py (analysis/)
- **Purpose:** Generates STL files for 3D printing of lattice targets.
- **Output:** `stl_outputs/` (solid, rectilinear, honeycomb, gyroid at 20/40/60%)
- **Resolution:** 0.1 mm voxels, marching cubes

### visualize_stls.py (analysis/)
- **Purpose:** Verification plots showing XY and XZ cross-sections.
- **Output:** `results/stl_verification/*.png`

### generate_geometry.py (scripts/)
- **Purpose:** Generates STL files for gyroid, voronoi, and rectilinear geometries for CADMesh import.
- **Output:** `geometry/*.stl`
- **Resolution:** 0.1 mm (gyroid/rectilinear), 0.2 mm (voronoi)

### generate_proposal_figures.py (analysis/)
- **Purpose:** Generates proposal-specific publication figures (figures 4-6).
- **Input:** `data/sweep0/n_scaling_summary.json`, `data/sweep0/gyroid_independent_summary.json`, ROOT files from `data/proposal/`
- **Output:** `proposal_figures/figure_{4,5,6}.{png,pdf}`

### generate_theory_comparison.py (analysis/)
- **Purpose:** Compares Highland, Gaussian mixture, and Geant4+Moliere models.
- **Input:** `data/proposal/rect_40pct_4GeV.root`
- **Output:** `proposal_figures/theory_same_mass_comparison.{png,pdf}`
- **Moliere parameters:** w=0.02 (tail weight), r=3.2 (tail/core width ratio)

### n_scaling_analysis.py (analysis/)
- **Purpose:** Analyzes universal 1/N kurtosis scaling for stacked independent layers.
- **Input:** `data/sweep0/{n_scaling,gyroid_period,gyroid_independent}_summary.json`
- **Output:** `results/n_scaling/n_scaling_universal.png`, `results/n_scaling/n_scaling_summary.json`

### bl4s_predictions.py (analysis/)
- **Purpose:** Generates BL4S 2026 measurement predictions and required event counts.
- **Input:** `data/sweep0/sweep0_summary.json`, `results/universal_final/universal_final_results.json`
- **Output:** `results/bl4s_predictions/bl4s_predictions.json`
- **Constants:** `KAPPA_M_G4 = {2.0: 4.394, 4.0: 3.616, 6.0: 4.097}`, `THIN_WALL_ENHANCEMENT = {2.0: 1.00, 4.0: 1.10, 6.0: 1.22}`, `N_eff = 30,000`

### make_all_figures.py (root)
- **Purpose:** Master script generating NIM-A publication figures 2-7 using **synthetic data** (not ROOT files).
- **Output:** `figure_{2-7}_*.png` in project root

### make_figure1.py (root)
- **Purpose:** Generates NIM-A Figure 1: 2x5 grid of geometry cross-sections at 0.05 mm resolution.
- **Output:** `figure_1_geometry_cross_sections.{png,pdf}` in project root

---

## 5. Existing Data Inventory

### JSON data files
| File | Location | Contents |
|------|----------|---------|
| `sweep0_summary.json` | `data/sweep0/` | 21 configs: 5 geom × 4 infills + solid control. Fields: geometry, infill, mean_x_over_X0, var_x_over_X0, predicted_kurtosis, hit_fraction, path_length_percentiles |
| `n_scaling_summary.json` | `data/sweep0/` | Stacked rectilinear 40% at N=1,2,3,5,10,20,50,100 layers |
| `gyroid_period_summary.json` | `data/sweep0/` | Deterministic gyroid period sweep (correlated phases) |
| `gyroid_independent_summary.json` | `data/sweep0/` | Independent random gyroid phases (confirms 1/N scaling) |

### NPZ data files (20 files in data/sweep0/)
Path-length distributions for 1M rays each:
- `paths_{rectilinear,honeycomb,gyroid,cubic,voronoi}_{20,40,60,80}pct.npz`

### STL files (3 files in proposal_targets/)
- `solid_100pct.stl`, `rectilinear_40pct.stl`, `gyroid_40pct.stl`

### PNG/PDF figures
**Root directory (7 PNG files):**
- `figure_1_geometry_cross_sections.png` — 10-panel geometry visualization
- `figure_2_angular_distributions.png` — Synthetic scattering angle PDFs
- `figure_3_kurtosis_vs_infill.png` — κ vs infill % with theory band
- `figure_4_measured_vs_predicted.png` — Measured vs predicted κ scatter
- `figure_5_thin_wall_enhancement.png` — Moliere enhancement ratios
- `figure_6_n_scaling.png` — Universal 1/N kurtosis scaling
- `figure_7_three_model_comparison.png` — Highland vs mixture vs Geant4

**proposal_figures/ (4 PNG + 4 PDF):**
- `figure_4.{png,pdf}` — N-scaling law
- `figure_5.{png,pdf}` — Angular distributions (Geant4)
- `figure_6.{png,pdf}` — Kurtosis vs infill at 4 GeV
- `theory_same_mass_comparison.{png,pdf}` — Model comparison

**proposal_targets/ (3 PNG):**
- `target_cross_sections.png`, `target_3d_visualization.png`, `target_zoomed_sections.png`

### Missing data
- **No ROOT files** — no Geant4 simulation outputs exist in the repo
- **No `results/` directory** — analysis pipeline has not been run
- **No `data/proposal/` directory** — proposal campaign has not been run

---

## 6. Build System

### CMakeLists.txt
- **CMake:** 3.16-3.27
- **Project:** `MCSHighland` v1.0.0, C++ only
- **Geant4:** Required, with optional `ui_all vis_all` (ON by default)
- **CADMesh:** Optional (`-DWITH_CADMESH=ON`), adds `-DUSE_CADMESH` define for STL import of gyroid/voronoi geometries. Falls back to voxelised construction if not found.
- **Source collection:** `file(GLOB sources src/*.cc)`, `file(GLOB headers include/*.hh)`
- **Executable:** `MCSHighland` from `MCSHighland.cc` + all src/include
- **Macros copied:** `macros/*.mac` → build directory
- **STL copied:** `geometry/*.stl` → build directory (but `geometry/` doesn't exist yet)
- **Install:** `MCSHighland` to `bin/`

### install_geant4.sh (scripts/)
- **Conda method (recommended):** Creates `g4highland` conda env with: geant4, root, cmake, compilers, python=3.12, numpy, scipy, matplotlib, scikit-image, uproot, awkward
- **Source method:** Downloads Geant4 11.2.2 from GitHub, builds with cmake, installs to `~/geant4/install`
- **Check mode:** `--check` verifies geant4-config, ROOT, Python packages
- **Python deps (source method):** numpy, scipy, matplotlib, scikit-image, numpy-stl, uproot, awkward

### run_all_sweeps.sh (scripts/)
- **Pipeline:**
  1. Build (`cmake .. && make`)
  2. Generate macros (`python3 scripts/generate_macros.py`)
  3. Run sweeps 0-6 + controls
  4. Run analysis (`python3 analysis/analyze_mcs.py`)
- **Options:** `--build-only`, `--sweep N` (run single sweep), `--jobs N` (parallel)
- **Sweep 0:** Ray-trace (no Geant4) via `scripts/ray_trace_sweep0.py`
- **Sweeps 1-6:** Run generated macros from `macros/generated/`
- **Controls:** `run_solid_control.mac`, `run_air_control.mac`
- **Output handling:** Moves `.root` and `.csv` files from build/ to data/

### run_proposal_campaign.sh (scripts/)
- **27 configurations:** 4 controls + 4×4 lattice infills + 2×2 energy scans
- **50k events per config** (except Voronoi: 200k)
- **Generates per-run macro files** in `data/proposal/`
- **Seed:** Deterministic from config name via `cksum`

---

## 7. Gaps and Missing Capabilities

### Gap 1: Run solid PLA slabs at arbitrary thicknesses independent of lattice mode
- **Status: ALREADY SUPPORTED**
- `/MCS/det/sampleThickness` works with `/MCS/det/geometry solid` to set arbitrary slab thickness (0.1-10+ mm)
- The parameter is independent of lattice geometry

### Gap 2: Switch material from PLA to silicon or tungsten
- **Status: NOT SUPPORTED**
- **File:** `src/DetectorConstruction.cc` (line 40-54, `DefineMaterials()`)
- **Change needed:** Add silicon (`G4_Si`) and tungsten (`G4_W`) materials from NIST. Add a `fMaterialName` string member, a `/MCS/det/material` messenger command, and a setter that selects the active material. Update all geometry constructors to use the selected material instead of hardcoded `fPLA`.
- **Estimated scope:** ~50 lines in DetectorConstruction, ~15 lines in DetectorMessenger

### Gap 3: Switch particle from electron to muon
- **Status: NOT SUPPORTED**
- **File:** `src/PrimaryGeneratorAction.cc` (line 17-19)
- **Change needed:** Add a `fParticleName` string member, a `/MCS/gun/particle` messenger command. In constructor, keep electron as default. In setter, use `G4ParticleTable::FindParticle(name)` to switch. The Highland formula in analysis scripts assumes beta≈1 which is fine for both 4 GeV electrons and muons.
- **Estimated scope:** ~20 lines in PrimaryGeneratorAction

### Gap 4: Switch EM physics from option4 to option0 or option3
- **Status: NOT SUPPORTED**
- **File:** `src/PhysicsList.cc` (line 24)
- **Change needed:** The EM physics is registered in the constructor with `new G4EmStandardPhysics_option4()`. This cannot be changed at runtime because physics lists are constructed before `/run/initialize`. Options:
  - Add a constructor parameter and set it from `main()` via a command-line flag
  - Or use `G4PhysListFactory` with a string name
  - Or add `ReplacePhysics()` in a pre-init messenger command (complex)
- **Estimated scope:** ~40 lines across PhysicsList.cc and MCSHighland.cc

### Gap 5: Build stacked independent layers (N rectilinear layers with random phase offsets)
- **Status: NOT SUPPORTED in Geant4** (only in ray-trace code)
- **File:** `src/DetectorConstruction.cc`
- **Change needed:** Add a `fNLayers` parameter and `/MCS/det/nLayers` command. In `ConstructRectilinearLattice()`, divide `fSampleThickness` into N equal z-slices, each with independently randomized wall positions (random x/y phase offset per layer). Similarly for other geometries.
- **Ray-trace version exists:** `scripts/ray_trace_sweep0.py` already has `raytrace_stacked_rectilinear()` and `raytrace_gyroid_stacked()` with independent phases — this just needs a Geant4 equivalent.
- **Estimated scope:** ~80 lines in DetectorConstruction (new method + parameter)

### ~~Gap 6: Re-run analysis with different cut thresholds without modifying source code~~
- **Status: RESOLVED**
- **File:** `analysis/analyze_mcs.py` — now supports configurable cuts via `--energy-cut-fraction`, `--angle-cut-sigma`, `--fiducial-x`, `--fiducial-y`, `--beam-energy`, and `--no-cuts` (backward compatible). Prints per-cut event removal summary.
- **File:** `analysis/proposal_analysis.py` — hardcoded constants replaced with CLI arguments `--energy-cut-fraction` (default 0.9), `--angle-cut-sigma` (default 10), `--fiducial-x` (default 5.0), `--fiducial-y` (default 10.0). Also accepts `--data-dir` and `--output-dir`. Prints cut configuration and per-file cut summary.

### Additional issues noted
1. **Class name typo:** `DetectorConstruction` is missing the 'o' in "Construction" — affects all 4 files (header, source, messenger header, messenger source, main). Cosmetic but worth noting.
2. ~~**Cubic lattice bug:**~~ **Resolved** — upon re-inspection, `ConstructCubicLattice()` is correct: `yBounds` is initialized at line 645, `ix` is a proper loop variable at line 657. No fix needed.
3. ~~**Duplicate script:**~~ **Fixed** — `analysis/ray_trace_sweep0.py` deleted; only `scripts/ray_trace_sweep0.py` remains (used by `run_all_sweeps.sh`).
4. ~~**sweep2_infill_scan.mac incomplete:**~~ **Fixed** — deleted the incomplete file; `generate_macros.py` creates the real sweep2 macros in `macros/generated/`.
5. ~~**Duplicate figure PNGs in project root:**~~ **Fixed** — deleted 7 duplicate `figure_*` PNGs from project root; canonical copies remain in `paper/`.
6. ~~**PLA X0 areal density error in paper:**~~ **Fixed** — `paper/main.tex` line 313 corrected from 43 g/cm² to 39 g/cm² (1.24 g/cm³ × 31.5 cm = 39.06 g/cm²).
